<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.csb.csb.analis_fin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/no_barra">
        <activity android:name=".bienvenido">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activo_balance" />
        <activity android:name=".benchmarking" />
        <activity android:name=".estado_resultados" />
        <activity android:name=".pasivo_balance" />
        <activity
            android:name=".cargar_analisis_financiero"
            android:label="ANÁLISIS FINANCIERO"
            android:theme="@style/no_barra"></activity>
    </application>

</manifest>